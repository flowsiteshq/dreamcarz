import DashboardShell from "@/components/DashboardShell";
import { CreditCard, ShieldCheck } from "lucide-react";
import { Link } from "wouter";

export default function Payments() {
  return <DashboardShell title="Payments"><div className="mx-auto max-w-4xl space-y-6"><section><h2 className="text-2xl font-bold text-black">Payments</h2><p className="mt-1 text-sm text-gray-500">Your authorized payment records will appear here after DreamCarz records them.</p></section><section className="grid gap-4 sm:grid-cols-3">{[['Payment records', 'No payment records to display', CreditCard], ['Payment method', 'Added during a protected payment step', ShieldCheck], ['Membership terms', 'Reviewed before enrollment', ShieldCheck]].map(([label, value, Icon]) => { const ItemIcon = Icon as typeof CreditCard; return <div key={label as string} className="border border-gray-200 bg-white p-5"><ItemIcon size={18} className="mb-3 text-[#a8832d]"/><p className="text-sm font-semibold text-black">{value as string}</p><p className="mt-1 text-[11px] text-gray-400">{label as string}</p></div>})}</section><section className="border border-gray-200 bg-[#f8f8f5] p-7"><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-[#a8832d]">Keep your terms clear</p><h3 className="mt-3 font-display text-2xl font-bold">Membership and vehicle charges are reviewed separately.</h3><p className="mt-3 max-w-2xl text-sm leading-relaxed text-gray-600">DreamCarz does not display a payment amount, account balance, or card detail until it is part of an authorized, account-owned payment record.</p><Link href="/pricing" className="mt-5 inline-flex text-sm font-semibold underline underline-offset-4">Compare membership paths</Link></section></div></DashboardShell>;
}
