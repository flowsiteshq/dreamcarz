import DashboardShell from "@/components/DashboardShell";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ClipboardCheck, CalendarCheck, Check, AlertTriangle, X, RefreshCw, ShieldCheck, FileText, Search, ArrowRight } from "lucide-react";
import { useMemo, useState } from "react";
import { TransactionQuoteManager } from "@/components/TransactionQuoteManager";

function formatDate(value?: Date | string | null) {
  return value ? new Date(value).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) : "Not submitted";
}

function maskedReference(value?: string | null) {
  return value ? `Recorded ••••${value.slice(-8)}` : "Not recorded";
}

function AdminTransactionEvidence({ detail }: { detail: any }) {
  const paymentEvents = detail.events.filter((event: any) => event.eventType.startsWith("payment") || event.eventType.startsWith("checkout") || event.eventType.includes("cocard"));
  return <section className="mt-5 grid gap-4 border-t border-gray-200 pt-5 lg:grid-cols-2"><div className="border border-gray-200 bg-[#fbfaf7] p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Payment evidence</p><p className="mt-2 text-sm font-semibold capitalize text-black">{detail.transaction.paymentStatus.replaceAll("_", " ")}</p><div className="mt-3 space-y-1 text-[11px] leading-5 text-gray-600"><p>Provider: <span className="font-semibold text-black">{detail.transaction.paymentProvider || "Not configured"}</span></p><p>Gateway transaction: {maskedReference(detail.transaction.paymentProviderTransactionId)}</p><p>Authorization reference: {maskedReference(detail.transaction.paymentProviderAuthorizationId)}</p><p>Customer-vault reference: {maskedReference(detail.transaction.paymentProviderCustomerVaultId)}</p><p>Approved product SKU: {detail.transaction.cocardProductSku || "Not recorded"}</p></div><div className="mt-4 border-t border-gray-200 pt-3">{paymentEvents.length ? paymentEvents.map((event: any) => <p key={event.id} className="text-[10px] leading-5 text-gray-500">{event.eventType.replaceAll("_", " ")} · {formatDate(event.createdAt)} · {event.actorType}</p>) : <p className="text-[10px] leading-5 text-gray-400">No payment provider events have been recorded. Raw card data, customer billing data, client secrets, and webhook payloads are never displayed here.</p>}</div></div><div className="border border-gray-200 bg-[#fbfaf7] p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Vehicle condition reports</p><div className="mt-3 space-y-3">{detail.conditionReports.length ? detail.conditionReports.map((report: any) => <article key={report.id} className="border border-gray-200 bg-white p-3"><div className="flex items-center justify-between gap-2"><p className="text-[11px] font-bold capitalize text-black">{report.stage} · {report.status}</p><p className="text-[10px] text-gray-500">{formatDate(report.createdAt)}</p></div><div className="mt-2 grid gap-1 text-[10px] leading-5 text-gray-600 sm:grid-cols-2"><p>Odometer: {report.odometerReading ?? "Not recorded"}</p><p>Fuel: {report.fuelLevel || "Not recorded"}</p></div>{report.notes && <p className="mt-2 text-[11px] leading-5 text-gray-600">{report.notes}</p>}{report.photoKeys && <p className="mt-2 text-[10px] font-semibold text-gray-500">Secure photo evidence is referenced in this report.</p>}</article>) : <p className="text-[11px] leading-5 text-gray-400">No pickup or return condition reports have been submitted.</p>}</div></div></section>;
}

function NativeSignatureEvidence({ detail }: { detail: any }) {
  const nativeAgreements = detail.agreements.filter((agreement: any) => agreement.signingMethod === "native_attestation");
  if (!nativeAgreements.length) return null;
  return <section className="mt-5 border border-gray-200 bg-[#fbfaf7] p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Native signature evidence</p><div className="mt-3 grid gap-3 md:grid-cols-2">{nativeAgreements.map((agreement: any) => <article key={agreement.id} className="border border-gray-200 bg-white p-3"><p className="text-[11px] font-bold capitalize text-black">{agreement.agreementType} · {agreement.version}</p><p className="mt-1 text-[10px] text-gray-600">Status: {agreement.status.replaceAll("_", " ")}</p><p className="mt-1 text-[10px] text-gray-600">Signer: {agreement.signerName || "Not signed"}</p><p className="mt-1 text-[10px] text-gray-600">Acknowledged: {formatDate(agreement.signerAcknowledgedAt)}</p><p className="mt-1 text-[10px] text-gray-600">Signed: {formatDate(agreement.signedAt)}</p><p className="mt-1 break-all text-[10px] text-gray-500">Integrity record: {agreement.signatureHash ? `••••${agreement.signatureHash.slice(-12)}` : "Not recorded"}</p></article>)}</div></section>;
}

function TransactionPricingApproval({ detail }: { detail: any }) {
  const utils = trpc.useUtils();
  const [pricingSummary, setPricingSummary] = useState(detail.transaction.pricingSnapshot || "");
  const [cocardProductSku, setCocardProductSku] = useState(detail.transaction.cocardProductSku || "");
  const savePricing = trpc.operations.setTransactionPricing.useMutation({ onSuccess: () => utils.operations.transactionDetail.invalidate({ reference: detail.transaction.reference }) });
  if (detail.transaction.currentStep !== "pricing") return null;
  return <section className="mt-5 border border-[#d8d1c4] bg-[#f7f5f0] p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[#a46c18]">Pricing approval required</p><p className="mt-2 text-xs leading-5 text-gray-600">Enter only the verified transaction-specific rate, deposit, approved fee treatment, and any timing terms supplied by DreamCarz. This field does not calculate or invent charges.</p><textarea value={pricingSummary} onChange={event => setPricingSummary(event.target.value)} rows={4} placeholder="Record the approved vehicle-specific pricing summary here." className="mt-3 w-full border border-gray-300 bg-white p-3 text-xs leading-5 text-black outline-none focus:border-black" /><label className="mt-3 block text-[11px] font-semibold uppercase tracking-wide text-gray-600">CoCard Product Manager SKU<input value={cocardProductSku} onChange={event => setCocardProductSku(event.target.value)} placeholder="Exact merchant product SKU, if checkout is needed" className="mt-1 h-9 w-full border border-gray-300 bg-white px-3 text-xs text-black outline-none focus:border-black" /></label><p className="mt-2 text-[11px] leading-5 text-gray-500">Hosted CoCard checkout can use only a pre-existing merchant product SKU. Leave this blank only while payment setup is intentionally pending.</p><button type="button" disabled={savePricing.isPending || pricingSummary.trim().length < 5} onClick={() => savePricing.mutate({ reference: detail.transaction.reference, pricingSummary, cocardProductSku: cocardProductSku.trim() || undefined })} className="mt-3 h-9 bg-black px-3 text-[11px] font-semibold text-white disabled:opacity-50">{savePricing.isPending ? "Saving…" : "Record approved pricing"}</button>{savePricing.error && <p className="mt-2 text-[11px] text-red-600">{savePricing.error.message}</p>}</section>;
}

function NativeAgreementTemplateManager() {
  const { user } = useAuth();
  const templates = trpc.transactions.listAgreementTemplates.useQuery(undefined, { enabled: user?.role === "admin", refetchOnWindowFocus: false });
  const [agreementType, setAgreementType] = useState<"rental" | "purchase">("rental");
  const [version, setVersion] = useState("");
  const [title, setTitle] = useState("");
  const [content, setContent] = useState("");
  const [approvalReference, setApprovalReference] = useState("");
  const [legalApprovalConfirmed, setLegalApprovalConfirmed] = useState(false);
  const createTemplate = trpc.transactions.createAgreementTemplate.useMutation({ onSuccess: () => { templates.refetch(); setVersion(""); setTitle(""); setContent(""); setApprovalReference(""); setLegalApprovalConfirmed(false); } });
  if (user?.role !== "admin") return null;
  const submit = async () => {
    if (!legalApprovalConfirmed) return;
    await createTemplate.mutateAsync({ agreementType, version, title, content, legalApprovalReference: approvalReference, legallyApproved: true, activate: true });
  };
  return <section className="mt-6 border border-[#ded8cf] bg-[#faf9f6] p-5 sm:p-6"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#B8860B]">Native document controls</p><h3 className="mt-1 text-[16px] font-bold text-black">Agreement template manager</h3><p className="mt-1 max-w-3xl text-[12px] leading-5 text-gray-500">Create a controlled native signing template only after counsel has approved its language. Replacing an active template does not alter previously prepared or signed transaction snapshots. Never enter the unreviewed addendum as an approved template.</p></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><label className="text-[11px] font-semibold text-gray-600">Document type<select value={agreementType} onChange={event => setAgreementType(event.target.value as "rental" | "purchase")} className="mt-1 h-10 w-full border border-gray-300 bg-white px-3 text-sm text-black"><option value="rental">Rental agreement</option><option value="purchase">Purchase agreement</option></select></label><label className="text-[11px] font-semibold text-gray-600">Immutable version<input value={version} onChange={event => setVersion(event.target.value)} placeholder="e.g., rental-2026.1" className="mt-1 h-10 w-full border border-gray-300 bg-white px-3 text-sm font-normal text-black" /></label><label className="text-[11px] font-semibold text-gray-600">Document title<input value={title} onChange={event => setTitle(event.target.value)} placeholder="DreamCarz Rental Agreement" className="mt-1 h-10 w-full border border-gray-300 bg-white px-3 text-sm font-normal text-black" /></label></div><label className="mt-3 grid gap-1 text-[11px] font-semibold text-gray-600">Counsel approval reference<input value={approvalReference} onChange={event => setApprovalReference(event.target.value)} placeholder="Matter, version, or written approval reference" className="h-10 border border-gray-300 bg-white px-3 text-sm font-normal text-black" /></label><label className="mt-3 grid gap-1 text-[11px] font-semibold text-gray-600">Approved document content<textarea value={content} onChange={event => setContent(event.target.value)} rows={8} placeholder="Use {{CUSTOMER_NAME}}, {{VEHICLE_NAME}}, and {{TRANSACTION_REFERENCE}} only for controlled transaction fields." className="border border-gray-300 bg-white p-3 text-sm font-normal leading-5 text-black" /></label><label className="mt-3 flex gap-2 text-[11px] leading-5 text-gray-600"><input checked={legalApprovalConfirmed} onChange={event => setLegalApprovalConfirmed(event.target.checked)} type="checkbox" className="mt-0.5 accent-black" />I confirm counsel has approved this exact language and the documented DreamCarz entity, version, and signing workflow.</label><button type="button" disabled={createTemplate.isPending || !legalApprovalConfirmed || content.trim().length < 40} onClick={() => void submit()} className="mt-4 h-10 bg-black px-4 text-xs font-semibold text-white disabled:opacity-50">{createTemplate.isPending ? "Saving template…" : "Activate approved template"}</button>{createTemplate.error && <p className="mt-3 text-xs text-red-600">{createTemplate.error.message}</p>}<div className="mt-5 space-y-2 border-t border-gray-200 pt-4">{templates.data?.map(template => <div key={template.id} className="flex flex-wrap items-center justify-between gap-3 border border-gray-200 bg-white p-3"><div><p className="text-[12px] font-bold text-black">{template.title} <span className="font-normal text-gray-500">· {template.version}</span></p><p className="mt-1 text-[10px] text-gray-500">{template.agreementType} · approval: {template.legalApprovalReference || "not recorded"}</p></div><span className={template.isActive ? "bg-black px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-white" : "border border-gray-300 px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-gray-500"}>{template.isActive ? "Active" : "Archived"}</span></div>)}{!templates.isLoading && !templates.data?.length && <p className="text-[11px] text-gray-400">No approved native agreement template exists yet.</p>}</div></section>;
}

export default function AdminOperations() {
  const { user } = useAuth();
  const utils = trpc.useUtils();
  const queueQuery = trpc.operations.getQueue.useQuery(undefined, { enabled: user?.role === "admin", refetchOnWindowFocus: false });
  const reviewApplication = trpc.operations.reviewApplication.useMutation({ onSuccess: () => utils.operations.getQueue.invalidate() });
  const reviewReservation = trpc.operations.reviewReservation.useMutation({ onSuccess: () => utils.operations.getQueue.invalidate() });
  const reviewServiceReport = trpc.operations.reviewServiceReport.useMutation({ onSuccess: () => utils.operations.getQueue.invalidate() });
  const partnerQuery = trpc.partners.adminList.useQuery(undefined, { enabled: user?.role === "admin" });
  const setPartnerActive = trpc.partners.setActive.useMutation({ onSuccess: () => partnerQuery.refetch() });
  const [applicationNotes, setApplicationNotes] = useState<Record<number, string>>({});
  const [reservationNotes, setReservationNotes] = useState<Record<number, string>>({});
  const [transactionSearch, setTransactionSearch] = useState("");
  const [selectedTransactionReference, setSelectedTransactionReference] = useState<string | null>(null);
  const transactionConsoleInput = useMemo(() => ({ query: transactionSearch.trim() || undefined }), [transactionSearch]);
  const transactionConsole = trpc.operations.transactionConsole.useQuery(transactionConsoleInput, { enabled: user?.role === "admin", refetchOnWindowFocus: false });
  const transactionDetailInput = useMemo(() => ({ reference: selectedTransactionReference || "transaction-unselected" }), [selectedTransactionReference]);
  const transactionDetail = trpc.operations.transactionDetail.useQuery(transactionDetailInput, { enabled: user?.role === "admin" && Boolean(selectedTransactionReference), refetchOnWindowFocus: false });
  const updateTransactionStatus = trpc.operations.updateTransactionStatus.useMutation({ onSuccess: () => { utils.operations.transactionConsole.invalidate(); transactionDetail.refetch(); } });
  const openTransactionRecord = trpc.operations.getTransactionRecordLink.useMutation({ onSuccess: result => window.open(result.url, "_blank", "noopener,noreferrer") });

  if (user?.role !== "admin") {
    return <DashboardShell title="Operations"><div className="rounded-3xl border border-gray-100 bg-white p-8 text-center"><ShieldCheck className="mx-auto mb-3 text-gray-300" size={28} /><h2 className="text-xl font-bold text-black" style={{ fontFamily: "var(--font-display)" }}>Administrator access required</h2><p className="mt-2 text-[13px] text-gray-400">This workspace is available only to DreamCarz administrators.</p></div></DashboardShell>;
  }

  const applications = queueQuery.data?.applications ?? [];
  const reservations = queueQuery.data?.reservations ?? [];
  const serviceReports = queueQuery.data?.serviceReports ?? [];
  const applicationsAwaitingReview = applications.filter(item => ["submitted", "under_review", "needs_attention"].includes(item.status)).length;
  const reservationRequestsAwaitingReview = reservations.filter(item => ["submitted", "under_review", "change_requested"].includes(item.status)).length;
  const transactionRows = transactionConsole.data ?? [];
  const transactionExceptions = transactionRows.filter(item => ["manual_review", "verification_pending", "eligibility_review"].includes(item.status)).length;
  const selectedTransaction = transactionDetail.data;

  return (
    <DashboardShell title="Operations">
      <div className="space-y-6">
        <div className="flex flex-wrap items-end justify-between gap-4">
          <div>
            <p className="text-[11px] font-bold uppercase tracking-[0.18em] text-[#B8860B]">DreamCarz operations</p>
            <h2 className="mt-1 text-2xl font-bold text-black" style={{ fontFamily: "var(--font-display)" }}>Member review queue</h2>
            <p className="mt-1 text-[13px] text-gray-400">Approve rental readiness, guide members who need attention, and confirm vehicle requests.</p>
          </div>
          <button onClick={() => queueQuery.refetch()} className="inline-flex items-center gap-2 rounded-full border border-gray-200 px-4 py-2 text-[12px] font-semibold text-black hover:border-gray-400"><RefreshCw size={13} className={queueQuery.isFetching ? "animate-spin" : ""} /> Refresh</button>
        </div>

        <div className="grid gap-3 sm:grid-cols-3">
          <div className="rounded-2xl bg-black p-5 text-white"><p className="text-[10px] uppercase tracking-wider text-white/50">Applications to review</p><p className="mt-2 text-3xl font-bold" style={{ fontFamily: "var(--font-display)" }}>{applicationsAwaitingReview}</p><p className="mt-1 text-[11px] text-white/50">Rental Setup submissions</p></div>
          <div className="rounded-2xl border border-gray-100 bg-white p-5"><p className="text-[10px] uppercase tracking-wider text-gray-400">Vehicle requests</p><p className="mt-2 text-3xl font-bold text-black" style={{ fontFamily: "var(--font-display)" }}>{reservationRequestsAwaitingReview}</p><p className="mt-1 text-[11px] text-gray-400">Awaiting availability review</p></div>
          <div className="rounded-2xl border border-gray-100 bg-white p-5"><p className="text-[10px] uppercase tracking-wider text-gray-400">Approved members</p><p className="mt-2 text-3xl font-bold text-black" style={{ fontFamily: "var(--font-display)" }}>{applications.filter(item => item.status === "approved").length}</p><p className="mt-1 text-[11px] text-gray-400">Eligible for requests</p></div>
        </div>

        <section className="border border-[#ded8cf] bg-[#faf9f6] p-5 sm:p-6">
          <div className="flex flex-wrap items-end justify-between gap-4"><div><div className="flex items-center gap-2"><FileText size={17} className="text-[#B8860B]" /><h3 className="text-[16px] font-bold text-black">Transaction console</h3></div><p className="mt-1 text-[12px] text-gray-500">Private rental and purchase records, provider states, manual-review exceptions, and the immutable activity trail.</p></div><div className="border border-gray-200 bg-white px-3 py-2"><label className="flex items-center gap-2"><Search size={14} className="text-gray-400" /><input value={transactionSearch} onChange={event => setTransactionSearch(event.target.value)} placeholder="Search reference, vehicle, customer…" className="w-52 bg-transparent text-[12px] outline-none" /></label></div></div>
          <div className="mt-4 grid gap-3 sm:grid-cols-3"><div className="border border-gray-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400">Open transactions</p><p className="mt-1 text-2xl font-bold text-black">{transactionRows.length}</p></div><div className="border border-gray-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400">Manual exceptions</p><p className="mt-1 text-2xl font-bold text-[#a46c18]">{transactionExceptions}</p></div><div className="border border-gray-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400">Provider integrations</p><p className="mt-1 text-xs font-semibold text-black">Configuration-gated</p></div></div>
          <div className="mt-5 overflow-x-auto border border-gray-200 bg-white"><table className="min-w-[860px] w-full text-left"><thead className="border-b border-gray-200 bg-[#f4f1eb]"><tr className="text-[10px] uppercase tracking-wider text-gray-500"><th className="px-4 py-3">Reference / customer</th><th className="px-4 py-3">Vehicle</th><th className="px-4 py-3">Lifecycle</th><th className="px-4 py-3">Verification</th><th className="px-4 py-3">Payment / agreement</th><th className="px-4 py-3"></th></tr></thead><tbody>{transactionRows.map(item => <tr key={item.id} className="border-b border-gray-100 last:border-0"><td className="px-4 py-3"><p className="text-[12px] font-bold text-black">{item.reference}</p><p className="text-[10px] text-gray-500">{item.customerName || "Customer"} · {item.customerEmail || "No email"}</p></td><td className="px-4 py-3 text-[12px] text-black">{item.vehicleName}<p className="mt-0.5 text-[10px] uppercase text-gray-500">{item.transactionType} · {item.membershipPlan || "No plan"}</p></td><td className="px-4 py-3"><span className="bg-black px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-white">{item.status.replaceAll("_", " ")}</span><p className="mt-1 text-[10px] text-gray-500">Step: {item.currentStep.replaceAll("_", " ")}</p></td><td className="px-4 py-3 text-[10px] text-gray-600">ID: {item.identityStatus.replaceAll("_", " ")}<br />License: {item.licenseStatus.replaceAll("_", " ")}<br />Eligibility: {item.eligibilityStatus.replaceAll("_", " ")}</td><td className="px-4 py-3 text-[10px] text-gray-600">Payment: {item.paymentStatus.replaceAll("_", " ")}<br />Agreement: {item.agreementStatus.replaceAll("_", " ")}<br />Insurance: {item.insuranceStatus.replaceAll("_", " ")}</td><td className="px-4 py-3"><button onClick={() => setSelectedTransactionReference(item.reference)} className="inline-flex items-center gap-1 text-[11px] font-bold text-black underline underline-offset-4">Review <ArrowRight size={12} /></button></td></tr>)}</tbody></table></div>
          {transactionConsole.isLoading && <p className="py-5 text-center text-[12px] text-gray-400">Loading transaction console…</p>}
          {!transactionConsole.isLoading && !transactionRows.length && <p className="py-5 text-center text-[12px] text-gray-400">No rental or purchase transactions match this search.</p>}
          {selectedTransaction && <div className="mt-5 border border-black bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#B8860B]">Selected transaction</p><h4 className="mt-1 text-lg font-bold text-black">{selectedTransaction.transaction.reference} · {selectedTransaction.transaction.vehicleName}</h4><p className="mt-1 text-[12px] text-gray-500">{selectedTransaction.customerName || "Customer"} · {selectedTransaction.customerEmail || "No email"}</p></div><button onClick={() => setSelectedTransactionReference(null)} className="text-[11px] font-semibold underline underline-offset-4">Close</button></div><div className="mt-5 grid gap-4 lg:grid-cols-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Lifecycle control</p><div className="mt-2 flex flex-wrap gap-2"><button disabled={updateTransactionStatus.isPending} onClick={() => updateTransactionStatus.mutate({ reference: selectedTransaction.transaction.reference, nextStatus: "manual_review", note: "Administrator requested manual transaction review." })} className="border border-amber-300 px-3 py-2 text-[11px] font-semibold text-amber-800 disabled:opacity-50">Send to manual review</button>{selectedTransaction.transaction.status === "agreement_pending" && <button disabled={updateTransactionStatus.isPending} onClick={() => updateTransactionStatus.mutate({ reference: selectedTransaction.transaction.reference, nextStatus: "ready_for_pickup", note: "Agreement and operational conditions reviewed by administrator." })} className="bg-black px-3 py-2 text-[11px] font-semibold text-white disabled:opacity-50">Ready for pickup / delivery</button>}{selectedTransaction.transaction.status === "ready_for_pickup" && <button disabled={updateTransactionStatus.isPending} onClick={() => updateTransactionStatus.mutate({ reference: selectedTransaction.transaction.reference, nextStatus: selectedTransaction.transaction.transactionType === "rental" ? "active_rental" : "completed", note: selectedTransaction.transaction.transactionType === "rental" ? "Vehicle handoff recorded by administrator." : "Purchase delivery recorded by administrator." })} className="bg-black px-3 py-2 text-[11px] font-semibold text-white disabled:opacity-50">{selectedTransaction.transaction.transactionType === "rental" ? "Record pickup" : "Record delivery"}</button>}</div><p className="mt-2 text-[10px] leading-4 text-gray-500">All state changes are validated on the server and appended to the audit trail.</p></div><div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Agreement & secure records</p><div className="mt-2 space-y-2">{selectedTransaction.agreements.map(agreement => <div key={agreement.id} className="flex items-center justify-between border border-gray-100 p-2"><span className="text-[11px] text-black">{agreement.agreementType} · {agreement.version} · {agreement.status}</span>{agreement.signedDocumentKey && <button onClick={() => openTransactionRecord.mutate({ transactionId: selectedTransaction.transaction.id, source: "agreement", recordId: agreement.id })} className="text-[10px] font-bold underline underline-offset-4">Open signed copy</button>}</div>)}{selectedTransaction.documents.map(document => <div key={document.id} className="flex items-center justify-between border border-gray-100 p-2"><span className="text-[11px] text-black">{document.documentType.replaceAll("_", " ")} · {document.status}</span><button onClick={() => openTransactionRecord.mutate({ transactionId: selectedTransaction.transaction.id, source: "document", recordId: document.id })} className="text-[10px] font-bold underline underline-offset-4">Open record</button></div>)}{!selectedTransaction.agreements.length && !selectedTransaction.documents.length && <p className="text-[11px] text-gray-400">No agreement or secure document is available yet.</p>}</div></div><div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-500">Audit trail</p><div className="mt-2 max-h-44 space-y-2 overflow-y-auto border border-gray-100 p-2">{selectedTransaction.events.map(event => <div key={event.id} className="border-b border-gray-100 pb-2 last:border-0"><p className="text-[10px] font-bold text-black">{event.eventType.replaceAll("_", " ")}</p><p className="text-[10px] text-gray-500">{formatDate(event.createdAt)} · {event.actorType}</p>{event.note && <p className="mt-0.5 text-[10px] text-gray-600">{event.note}</p>}</div>)}{!selectedTransaction.events.length && <p className="text-[11px] text-gray-400">No audit events recorded.</p>}</div></div></div>{openTransactionRecord.error && <p className="mt-3 text-[11px] text-red-600">{openTransactionRecord.error.message}</p>}{updateTransactionStatus.error && <p className="mt-3 text-[11px] text-red-600">{updateTransactionStatus.error.message}</p>}</div>}
          {selectedTransaction && <AdminTransactionEvidence detail={selectedTransaction} />}
          {selectedTransaction && <NativeSignatureEvidence detail={selectedTransaction} />}
          {selectedTransaction && <TransactionPricingApproval detail={selectedTransaction} />}
          {selectedTransaction && <TransactionQuoteManager detail={selectedTransaction} />}
        </section>

        <NativeAgreementTemplateManager />

        <section className="rounded-3xl border border-gray-100 bg-white p-5 sm:p-6">
          <div className="mb-5 flex items-center gap-2"><ClipboardCheck size={17} className="text-[#B8860B]" /><h3 className="text-[16px] font-bold text-black">Rental Setup approvals</h3></div>
          {queueQuery.isLoading ? <p className="py-8 text-center text-[13px] text-gray-400">Loading applications…</p> : applications.length ? <div className="space-y-4">{applications.map((application) => (
            <div key={application.id} className="rounded-2xl border border-gray-100 p-4">
              <div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[14px] font-bold text-black">{application.memberName || "Member"}</p><p className="text-[11px] text-gray-400">{application.memberEmail || "No email"} · Submitted {formatDate(application.submittedAt)}</p></div><span className="rounded-full bg-gray-100 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-gray-600">{application.status.replaceAll("_", " ")}</span></div>
              <div className="mt-3 grid gap-2 text-[12px] text-gray-500 sm:grid-cols-3"><p>Identity: <span className="font-semibold text-black">{application.identityVerificationStatus.replaceAll("_", " ")}</span></p><p>Requested: <span className="font-semibold text-black">{application.requestedStartDate || "—"}</span></p><p>Pickup: <span className="font-semibold text-black">{application.pickupLocation || "—"}</span></p></div>
              <textarea value={applicationNotes[application.id] ?? application.reviewNote ?? ""} onChange={event => setApplicationNotes(current => ({ ...current, [application.id]: event.target.value }))} placeholder="Optional member-facing review note" rows={2} className="mt-3 w-full rounded-xl border border-gray-100 bg-gray-50 px-3 py-2 text-[12px] outline-none focus:border-gray-300" />
              <div className="mt-3 flex flex-wrap gap-2"><button onClick={() => reviewApplication.mutate({ id: application.id, status: "approved", reviewNote: applicationNotes[application.id] })} className="inline-flex items-center gap-1.5 rounded-full bg-black px-3.5 py-2 text-[11px] font-semibold text-white"><Check size={12} /> Approve</button><button onClick={() => reviewApplication.mutate({ id: application.id, status: "needs_attention", reviewNote: applicationNotes[application.id] })} className="inline-flex items-center gap-1.5 rounded-full border border-amber-200 px-3.5 py-2 text-[11px] font-semibold text-amber-800"><AlertTriangle size={12} /> Needs Attention</button><button onClick={() => reviewApplication.mutate({ id: application.id, status: "declined", reviewNote: applicationNotes[application.id] })} className="inline-flex items-center gap-1.5 rounded-full border border-red-100 px-3.5 py-2 text-[11px] font-semibold text-red-600"><X size={12} /> Decline</button></div>
            </div>))}</div> : <p className="py-8 text-center text-[13px] text-gray-400">No rental applications have been submitted yet.</p>}
        </section>

        <section className="rounded-3xl border border-gray-100 bg-white p-5 sm:p-6">
          <div className="mb-5 flex items-center gap-2"><CalendarCheck size={17} className="text-[#B8860B]" /><h3 className="text-[16px] font-bold text-black">Vehicle request review</h3></div>
          {queueQuery.isLoading ? <p className="py-8 text-center text-[13px] text-gray-400">Loading vehicle requests…</p> : reservations.length ? <div className="space-y-4">{reservations.map((reservation) => (
            <div key={reservation.id} className="rounded-2xl border border-gray-100 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[14px] font-bold text-black">{reservation.vehicleName}</p><p className="text-[11px] text-gray-400">{reservation.reference} · {reservation.memberName || "Member"} · {reservation.memberEmail || "No email"}</p></div><span className="rounded-full bg-gray-100 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-gray-600">{reservation.status.replaceAll("_", " ")}</span></div><div className="mt-3 grid gap-2 text-[12px] text-gray-500 sm:grid-cols-3"><p>Dates: <span className="font-semibold text-black">{reservation.requestedStartDate} – {reservation.requestedEndDate}</span></p><p>Pickup: <span className="font-semibold text-black">{reservation.pickupLocation}</span></p><p>Phone: <span className="font-semibold text-black">{reservation.contactPhone}</span></p></div>{reservation.notes && <p className="mt-3 rounded-xl bg-gray-50 px-3 py-2 text-[12px] text-gray-500">Member note: {reservation.notes}</p>}<textarea value={reservationNotes[reservation.id] ?? reservation.reviewNote ?? ""} onChange={event => setReservationNotes(current => ({ ...current, [reservation.id]: event.target.value }))} placeholder="Optional member-facing availability note" rows={2} className="mt-3 w-full rounded-xl border border-gray-100 bg-gray-50 px-3 py-2 text-[12px] outline-none focus:border-gray-300" /><div className="mt-3 flex flex-wrap gap-2"><button onClick={() => reviewReservation.mutate({ id: reservation.id, status: "confirmed", reviewNote: reservationNotes[reservation.id] })} className="inline-flex items-center gap-1.5 rounded-full bg-black px-3.5 py-2 text-[11px] font-semibold text-white"><Check size={12} /> Confirm</button><button onClick={() => reviewReservation.mutate({ id: reservation.id, status: "change_requested", reviewNote: reservationNotes[reservation.id] })} className="inline-flex items-center gap-1.5 rounded-full border border-amber-200 px-3.5 py-2 text-[11px] font-semibold text-amber-800"><AlertTriangle size={12} /> Request Change</button><button onClick={() => reviewReservation.mutate({ id: reservation.id, status: "declined", reviewNote: reservationNotes[reservation.id] })} className="inline-flex items-center gap-1.5 rounded-full border border-red-100 px-3.5 py-2 text-[11px] font-semibold text-red-600"><X size={12} /> Decline</button></div></div>))}</div> : <p className="py-8 text-center text-[13px] text-gray-400">No vehicle requests have been submitted yet.</p>}
        </section>
        <section className="rounded-3xl border border-gray-100 bg-white p-5 sm:p-6">
          <div className="mb-5 flex items-center gap-2"><AlertTriangle size={17} className="text-[#B8860B]" /><h3 className="text-[16px] font-bold text-black">Service & incident reports</h3></div>
          {serviceReports.length ? <div className="space-y-3">{serviceReports.map(report => <div key={report.id} className="rounded-2xl border border-gray-100 p-4"><div className="flex justify-between gap-3"><div><p className="text-[14px] font-bold text-black">{report.category} · {report.vehicleName}</p><p className="text-[11px] text-gray-400">{report.reference} · {report.memberName || "Member"} · {formatDate(report.createdAt)}</p></div><span className="text-[10px] font-bold uppercase text-red-600">{report.urgency} · {report.status.replaceAll("_", " ")}</span></div><p className="mt-3 text-[12px] text-gray-600">{report.description}</p>{report.history.length ? <div className="mt-3 border-l-2 border-gray-100 pl-3">{report.history.map(event => <div key={event.id} className="mb-2 last:mb-0"><p className="text-[10px] font-bold uppercase tracking-wide text-gray-500">{event.status.replaceAll("_", " ")} · {formatDate(event.createdAt)}</p>{event.note && <p className="mt-0.5 text-[11px] text-gray-500">{event.note}</p>}</div>)}</div> : null}<div className="mt-3 flex gap-2"><button onClick={() => reviewServiceReport.mutate({ id: report.id, status: "under_review" })} className="rounded-full bg-black px-3 py-2 text-[11px] font-semibold text-white">Review</button><button onClick={() => reviewServiceReport.mutate({ id: report.id, status: "assigned" })} className="rounded-full border border-amber-200 px-3 py-2 text-[11px] font-semibold text-amber-800">Assign</button><button onClick={() => reviewServiceReport.mutate({ id: report.id, status: "resolved" })} className="rounded-full border border-green-200 px-3 py-2 text-[11px] font-semibold text-green-700">Resolve</button></div></div>)}</div> : <p className="py-8 text-center text-[13px] text-gray-400">No service or incident reports have been submitted yet.</p>}
        </section>
        <section className="rounded-3xl border border-gray-100 bg-white p-5 sm:p-6"><div className="mb-4 flex items-center justify-between"><h3 className="text-[16px] font-bold text-black">Partner directory</h3><span className="text-[11px] text-gray-400">{partnerQuery.data?.length || 0} listings</span></div><div className="space-y-2">{partnerQuery.data?.map(partner => <div key={partner.id} className="flex items-center justify-between rounded-2xl bg-gray-50 p-3"><div><p className="text-[12px] font-bold text-black">{partner.name}</p><p className="text-[10px] text-gray-400">{partner.category} · {partner.city}, {partner.state}</p></div><button onClick={() => setPartnerActive.mutate({ id: partner.id, isActive: !Boolean(partner.isActive) })} className="rounded-full border border-gray-200 px-3 py-1.5 text-[10px] font-semibold text-black">{partner.isActive ? "Deactivate" : "Activate"}</button></div>) || <p className="text-[12px] text-gray-400">No managed partner listings yet.</p>}</div></section>
      </div>
    </DashboardShell>
  );
}
